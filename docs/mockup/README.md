# Mockup assets

Drop the redesign reference image at:

```
docs/mockup/home-reference.png
```

It is referenced from `docs/REDESIGN_PRD.md` (section 0) and will render inline on GitHub once committed.

Suggested commit:

```bash
git add docs/mockup/home-reference.png
git commit -m "docs: add redesign reference mockup"
git push
```
